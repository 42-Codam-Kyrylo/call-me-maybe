# call-me-maybe

It is forbidden to use any private methods or attributes from the llm_sdk package.


 uv run python3 -m src --functions_definition=data/own/functions_definition.json --input=data/own/function_calling_tests.json  